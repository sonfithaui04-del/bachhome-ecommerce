server:
  port: 8085
spring:
  application:
    name: service-inventory
  datasource:
    url: jdbc:postgresql://localhost:5436/bachhome_inventory
    username: postgres
    password: 10112004
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
