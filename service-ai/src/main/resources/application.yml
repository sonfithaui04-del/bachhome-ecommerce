server:
  port: 8087

spring:
  application:
    name: service-ai

# Cấu hình Gemini (khóa API lấy từ biến môi trường GEMINI_API_KEY)
gemini:
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:gemini-2.5-flash}
  base-url: https://generativelanguage.googleapis.com/v1beta

# Cấu hình chatbot
ai:
  shop-name: BachHome
  shop-desc: "cửa hàng đồ gia dụng"
  use-product-context: true
  # Số sản phẩm chi tiết đưa vào mỗi lượt hỏi. Kho có vài trăm món nên không gửi hết:
  # service chọn những món sát câu hỏi nhất, kèm một đoạn tóm tắt toàn cửa hàng.
  max-products: 40
  # Bao lâu thì hỏi lại service-product một lần (giây). Giữa hai lần dùng bản trong bộ nhớ.
  cache-seconds: 300

# Eureka Client Configuration
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

# Logging
logging:
  level:
    com.bachhome.ai: DEBUG
  pattern:
    console: "[%d{yyyy-MM-dd HH:mm:ss}] [%level] [AI-SERVICE] %msg%n"

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
